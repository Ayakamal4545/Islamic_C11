import 'package:flutter/material.dart';

class AhdethTab extends StatelessWidget {
  const AhdethTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
    );
  }
}
